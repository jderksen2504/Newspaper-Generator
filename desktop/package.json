{
  "name": "newspaper-generator",
  "version": "1.4.0",
  "description": "Newspaper Generator für D&D Kampagnen — Tauri 2.0 + Playwright Sidecar",
  "license": "GPL-3.0-only",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "sidecar:install": "cd src-tauri/sidecars/playwright-export && npm install && npx playwright install chromium"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.10",
    "@tauri-apps/cli": "^2.0.0"
  }
}
